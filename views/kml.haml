!!! XML
%kml{:xmlns => 'http://www.opengis.net/kml/2.2'}
  %Document
    - @libraries.select{|l| l.is_a?(Library) && l.lat?}.each do |library|
      %Placemark
        %name= h library.name
        %Point
          %coordinates= "#{library.lng},#{library.lat}"
